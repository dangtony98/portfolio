import React from 'react';

export default ({ title }) => {
    return (
        <div className="resume-sub-heading">
            <h3 className="resume-sub-heading__title">{title}</h3>
        </div>
    );
}
